const wheatherScorer = () => {
	let wheather = ['rainy', 'sunny', 'overcast']
	let result = math.floor(math.random(wheather.length*3))
	if(result == rainy) {
		console.log(1)
	}
		if(result == sunny) {
		console.log(-1)
	}
		if(result == overcast) {
		console.log(0)
	}
	else{
		console.log('winter')
	}
	
}

console.log(wheatherScorer(result))